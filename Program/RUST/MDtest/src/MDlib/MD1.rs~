fn determineDim() {
let NDIM: i32 = 2;
}

type real = f64;
struct xy {
    x: real,
    y: real,
}

type VecR = xy;
struct Mol {
    r: VecR,
    rv: VecR,
    ra: VecR,
}

type newMol = Mol;
fn ComputeForces() {
    let dr = VecR {x: 0.0, y: 0.0};
    let (j1, j2, n) : (i32,i32,i32);
    rrCut = rCut.powf(2.0); // raise power of rCut to 2.0

    for () {

    }
    
}

fn main () {

}
